extends ../layout/master
block content
    .row
        .col-md-12
            .card
                .card-body
                    form(action="/tags",method="POST")
                        .form-group
                            label(for="name") Tags Name
                            input.form-control(type="text",name="name",placeholder='Tags name')
                        .float-right
                            button.btn.btn-info(type="submit") Save